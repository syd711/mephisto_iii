package de.calette.mephisto3.control;

/**
 * Enum that represents the different input methods provided by a single rotary encoder.
 */
public enum ControlEvent {
  PUSH, LONG_PUSH, NEXT, PREVIOUS;
}
